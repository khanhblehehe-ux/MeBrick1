{
  "buildCommand": "cd f && npm run build",
  "outputDirectory": "f/.next",
  "framework": "nextjs",
  "env": {
    "API_BACKEND_URL": {
      "required": true
    },
    "NEXT_PUBLIC_API_URL": {
      "required": true
    }
  }
}
